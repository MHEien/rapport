---
description: Push database schema changes to PostgreSQL
---

# Database Push

// turbo-all

Push Prisma schema to the database:

```bash
bunx prisma db push
```

> **Note:** Use `db push` for development. Only use `migrate dev` when explicitly asked.

To open Prisma Studio for data inspection:

```bash
bunx prisma studio
```
